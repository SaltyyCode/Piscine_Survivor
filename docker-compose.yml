services:
  postgres:
    image: postgres:17.6
    container_name: jeb_database
    env_file:
      - ./backend/.env
    volumes:
      - ./pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    restart: unless-stopped
